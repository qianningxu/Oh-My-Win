import AppKit

extension GeistColorTokens {
    // Sampled in sRGB from the reference sidebar and its selected row.
    static let sidebar100Dark = NSColor(srgbRed: 54.0 / 255, green: 57.0 / 255, blue: 85.0 / 255, alpha: 1)
    static let sidebar200Dark = NSColor(srgbRed: 70.0 / 255, green: 81.0 / 255, blue: 101.0 / 255, alpha: 1)
}
