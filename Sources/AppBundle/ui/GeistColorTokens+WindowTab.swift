import AppKit

extension GeistColorTokens {
    // Muted sidebar colors, expressed in sRGB so display color management
    // preserves the sampled purple/blue balance.
    static let windowTab100Dark = NSColor(srgbRed: 53.0 / 255, green: 51.0 / 255, blue: 85.0 / 255, alpha: 1)
    static let windowTab200Dark = NSColor(srgbRed: 49.0 / 255, green: 58.0 / 255, blue: 80.0 / 255, alpha: 1)
    static let windowTab100Light = NSColor(srgbRed: 224.0 / 255, green: 245.0 / 255, blue: 250.0 / 255, alpha: 1)
    static let windowTab200Light = NSColor(srgbRed: 237.0 / 255, green: 240.0 / 255, blue: 246.0 / 255, alpha: 1)
}
